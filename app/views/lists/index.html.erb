<h1>hello from index</h1>
<ul>
<% @lists.each do |list|  %>

   <li> <%=link_to list.name , list_path(list), class:"btn btn-primary m-2"%> </li>
<% end %>

<%= link_to "Add a new List", new_list_path , class: "btn btn-primary mb-5" %>
</ul>
